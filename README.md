# Dynamic-Translation-Optimization-Pintool
A small pintool that optimizes program's runtime, by performing multiple loop unrolling optimizations.

Compile (make sure intel's Pin has been correctly installed):

`make`

Run:

`./run.sh`
